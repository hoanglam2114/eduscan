# ===================================================================
# DATABASE CONFIGURATION (MySQL)
# ===================================================================
# Replace "eduscan_db" with the database name you want to use
spring.datasource.url=jdbc:mysql://localhost:3306/edu_scan?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# Replace with your MySQL username
spring.datasource.username=root
# Replace with your MySQL password
spring.datasource.password=eduscan@123
# Config to fit with created db deployed on server